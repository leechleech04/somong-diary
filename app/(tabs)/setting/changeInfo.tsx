import Header from '@/components/Header';
import { BasicContainer } from '@/utils/utilComponents';

const changeInfo = () => {
  return (
    <BasicContainer>
      <Header title="이메일/비밀번호 변경" />
    </BasicContainer>
  );
};

export default changeInfo;
